import io;

function int remainder(int dividend, int divisor) {
    return dividend - ((dividend/divisor) * divisor);
}

function void main() {
    let int dividend = io.read_int();
    io.print(remainder(dividend, 5));
}